name: sales

x-environment:
  # Database settings
  &de
  WEB_IMAGE: "${WEB_IMAGE:-debeski/dlux-crm:sales}"
  POSTGRES_DB: "${POSTGRES_DB:-sales_db}"
  POSTGRES_PORT: "5432"
  POSTGRES_HOST: "db"
  # Redis / Celery
  REDIS_URL_DB: "redis://redis:6379/1"
  CELERY_BROKER_URL: "redis://redis:6379/2"
  CELERY_RESULT_BACKEND: "redis://redis:6379/3"
  # Backup settings
  BACKUP_INTERVAL: "${BACKUP_INTERVAL:-680400}"
  # Web app settings (override these in compose.dev.yml or .env)
  # Django/DLux owns the public/staff path split on the canonical host.
  # The legacy erp.<domain> host may redirect to /staff/ at Caddy.
  ALLOWED_HOSTS: "${ALLOWED_HOSTS:-web,localhost,127.0.0.1,switchlibya.ly,www.switchlibya.ly,erp.switchlibya.ly,caddy}"
  ALLOWED_URLS: "${ALLOWED_URLS:-https://switchlibya.ly,https://www.switchlibya.ly}"
  BASE_URL: "${BASE_URL:-https://switchlibya.ly}"
  DEBUG_STATUS: "${DEBUG_STATUS:-False}"
  # DjangoLux verified inline updater (generated Compose deployments only)
  DLUX_INLINE_UPDATES_ENABLED: "True"
  DLUX_UPDATE_CHECK_INTERVAL: "${DLUX_UPDATE_CHECK_INTERVAL:-86400}"
  DLUX_UPDATE_RUNTIME_ROOT: "/opt/dlux-runtime"
  # App email settings. The app talks to the internal SMTP relay; only the relay has public egress.
  EMAIL_BACKEND: "django.core.mail.backends.smtp.EmailBackend"
  EMAIL_HOST: "smtp-relay"
  EMAIL_PORT: "1025"
  EMAIL_USE_TLS: "False"
  EMAIL_USE_SSL: "False"
  DEFAULT_FROM_EMAIL: "${DEFAULT_FROM_EMAIL:-webmaster@localhost}"

services:
  db:
    image: postgres:17
    restart: always
    labels:
      org.dlux.restart: "protected"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      <<: *de
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin_pass}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - internal

  redis:
    image: redis:7
    restart: always
    labels:
      org.dlux.restart: "protected"
    command: ["redis-server", "--appendonly", "yes", "--appendfsync", "everysec", "--maxmemory", "500mb", "--maxmemory-policy", "allkeys-lru"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - internal

  smtp-relay:
    image: ${WEB_IMAGE:-debeski/dlux-crm:sales}
    restart: always
    labels:
      org.dlux.restart: "safe"
    entrypoint: []
    command: ["python", "-m", "dlux.smtp_relay"]
    environment:
      <<: *de
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin_pass}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-local_secret}
      DJANGO_SETTINGS_MODULE: "config.settings"
      SMTP_RELAY_LISTEN_HOST: "0.0.0.0"
      SMTP_RELAY_LISTEN_PORT: "1025"
      SMTP_RELAY_HOST: "${SMTP_RELAY_HOST:-smtp.gmail.com}"
      SMTP_RELAY_PORT: "${SMTP_RELAY_PORT:-587}"
      SMTP_RELAY_USE_TLS: "${SMTP_RELAY_USE_TLS:-True}"
      SMTP_RELAY_USER: "${SMTP_RELAY_USER:-}"
      SMTP_RELAY_PASSWORD: "${SMTP_RELAY_PASSWORD:-}"
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; socket.create_connection(('127.0.0.1', 1025), 5).close()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    networks:
      - egress
      - internal


  caddy:
    image: caddy:latest
    restart: always
    labels:
      org.dlux.restart: "safe"
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    environment:
      # Canonical Django/DLux host(s), space-separated; Caddy auto-provisions certs.
      # e.g. "switchlibya.ly www.switchlibya.ly"
      CADDY_SITE_ADDRESS: "${CADDY_SITE_ADDRESS:-switchlibya.ly www.switchlibya.ly}"
      # Optional legacy ERP hostname redirected to CADDY_PRIMARY_URL/staff/.
      CADDY_ERP_ADDRESS: "${CADDY_ERP_ADDRESS:-erp.switchlibya.ly}"
      CADDY_PRIMARY_URL: "${CADDY_PRIMARY_URL:-https://switchlibya.ly}"
      # Max upload size (Caddy units, e.g. 10MB).
      CADDY_MAX_SIZE: "${CADDY_MAX_SIZE:-10MB}"
    volumes:
      - ./.proxy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data        # certs + ACME account — MUST persist across restarts
      - caddy_config:/config
      - static:/app/staticfiles:ro
      - ./media/:/app/media:ro
      - dlux_runtime:/opt/dlux-runtime:ro
      - ./.proxy/maintenance.html:/usr/share/caddy/maintenance/maintenance.html:ro
    depends_on:
      # Not gated on web health / no restart:true — Caddy re-resolves web per dial,
      # so it stays up and keeps serving the maintenance progress page + /_update/*
      # polling while web is recreated during an image update.
      web:
        condition: service_started
    healthcheck:
      test: ["CMD", "caddy", "validate", "--adapter", "caddyfile", "--config", "/etc/caddy/Caddyfile"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - frontend
      - internal

  web:
    image: ${WEB_IMAGE:-debeski/dlux-crm:sales}
    restart: always
    labels:
      org.dlux.restart: "safe"
    command: >
      python -m dlux.updater.supervisor -- bash -c ' if [ "$$DEBUG_STATUS" = "True" ]; then
          echo "Starting in DEBUG mode with Django development server..."
          python manage.py runserver 0.0.0.0:8000
      else
          echo "Starting in PRODUCTION mode with Gunicorn..."
          gunicorn -c gunicorn.py config.wsgi:application
      fi'
    entrypoint: ["/app/entrypoint.sh"]
    volumes:
      - static:/app/staticfiles:rw
      - ./media/:/app/media:rw
      - ./logs/:/app/logs:rw
      - ./imports/:/app/imports:ro
      - dlux_runtime:/opt/dlux-runtime:ro
    environment:
      <<: *de
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin_pass}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-local_secret}
      ADMIN_PASS: ${ADMIN_PASS:-admin}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/', timeout=5).read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      smtp-relay:
        condition: service_healthy
    networks:
      - internal

  celery:
    image: ${WEB_IMAGE:-debeski/dlux-crm:sales}
    restart: always
    labels:
      org.dlux.restart: "safe"
    command: ["python", "-m", "dlux.updater.supervisor", "--", "python", "-m", "celery", "-A", "config", "worker", "-B", "--loglevel=info"]
    entrypoint: ["/app/entrypoint.sh"]
    pre_start:
      - environment:
          DLUX_BOOT_GATE: "off"
        command: ["python", "-m", "dlux.updater.supervisor", "--no-watch", "--", "python", "manage.py", "dlux_reconcile"]
      - environment:
          DLUX_BOOT_GATE: "off"
        command: ["sh", "-c", "python -m dlux.updater.supervisor --no-watch -- python manage.py migrator ${DLUX_MIGRATOR_FLAGS:-}"]
    volumes:
      - static:/app/staticfiles:rw
      - ./media/:/app/media:rw
      - ./logs/:/app/logs:rw
      - dlux_runtime:/opt/dlux-runtime:rw
    environment:
      <<: *de
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin_pass}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-local_secret}
      ADMIN_PASS: ${ADMIN_PASS:-admin}
    healthcheck:
      test: ["CMD", "python", "-m", "celery", "-A", "config", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - internal
      # Public egress so the periodic rate-scrape task (finance.tasks.refresh_market_rates)
      # can reach cbl.gov.ly / eanlibya.com. web stays internal-only and reads the
      # scraped rates from the shared Redis cache.
      - egress

  # DjangoLux Composer agent start
  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:latest
    restart: always
    labels:
      org.dlux.restart: "protected"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      CONTAINERS: 1
      IMAGES: 1
      EVENTS: 1
      INFO: 1
      PING: 1
      VERSION: 1
      NETWORKS: 0
      VOLUMES: 0
      POST: 0
      EXEC: 0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - docker_proxy

  composer-executor:
    image: debeski/composer:latest
    restart: unless-stopped
    labels:
      org.dlux.restart: "protected"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    # Read-only file override so this uncapped UID-0 process can read the
    # project's 0600 .secrets/.env to deploy. No write/exec/setuid bypass.
    cap_add:
      - DAC_READ_SEARCH
    working_dir: "${PWD}"
    command:
      - executor
      - --socket
      - /run/composer-exec/composer-exec.sock
      - --trigger-file
      - /opt/dlux-runtime/state/image-update-request.json
      - --status-file
      - /opt/dlux-runtime/state/deploy-status.json
      - --interval
      - "2"
    environment:
      WEB_IMAGE: "${WEB_IMAGE:-debeski/dlux-crm:sales}"
      COMPOSER_EXECUTOR_SOCKET: "/run/composer-exec/composer-exec.sock"
      COMPOSER_VERSION_LABEL: "org.dlux_crm.dlux_baked_version"
      COMPOSER_RELEASE_MANIFEST_LABEL: "org.dlux.project.release-manifest"
      COMPOSER_ACTIVE_VERSION_FILE: "/opt/dlux-runtime/state/active.json"
      COMPOSER_ACTIVE_VERSION_KEY: "version"
      COMPOSER_STATUS_FILE: "/opt/dlux-runtime/state/deploy-status.json"
      COMPOSER_WATCH_SELF_SERVICE: "composer-executor"
      COMPOSER_EXCLUDE_SERVICES: "composer-agent,composer-executor,docker-socket-proxy,db,redis"
      COMPOSER_AGENT_RESTART_SERVICES: "web,celery,smtp-relay,caddy"
    volumes:
      - "${PWD}:${PWD}:ro"
      - dlux_runtime:/opt/dlux-runtime:rw
      - composer_exec_sock:/run/composer-exec:rw
      - /var/run/docker.sock:/var/run/docker.sock:rw
    networks:
      - docker_proxy

  composer-agent:
    image: debeski/composer:latest
    restart: unless-stopped
    labels:
      org.dlux.restart: "protected"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    working_dir: "${PWD}"
    command:
      - agent
      - --trigger-file
      - /opt/dlux-runtime/state/image-update-request.json
      - --status-file
      - /opt/dlux-runtime/state/deploy-status.json
      - --bridge-dir
      - /opt/dlux-runtime/state/agent
      - --interval
      - "2"
      - --check-image
      - ${WEB_IMAGE:-debeski/dlux-crm:sales}
      - --availability-file
      - /opt/dlux-runtime/state/image-available.json
      - --check-interval
      - "3600"
    environment:
      DOCKER_HOST: "tcp://docker-socket-proxy:2375"
      WEB_IMAGE: "${WEB_IMAGE:-debeski/dlux-crm:sales}"
      COMPOSER_CONTROL_URL: "${COMPOSER_CONTROL_URL:-}"
      COMPOSER_ENROLLMENT_TOKEN: "${COMPOSER_ENROLLMENT_TOKEN:-}"
      COMPOSER_AGENT_STATE_DIR: "/var/lib/composer-agent"
      COMPOSER_EXECUTOR_SOCKET: "/run/composer-exec/composer-exec.sock"
      COMPOSER_VERSION_LABEL: "org.dlux_crm.dlux_baked_version"
      COMPOSER_RELEASE_MANIFEST_LABEL: "org.dlux.project.release-manifest"
      COMPOSER_ACTIVE_VERSION_FILE: "/opt/dlux-runtime/state/active.json"
      COMPOSER_ACTIVE_VERSION_KEY: "version"
      COMPOSER_STATUS_FILE: "/opt/dlux-runtime/state/deploy-status.json"
      COMPOSER_WATCH_SELF_SERVICE: "composer-agent"
      COMPOSER_EXCLUDE_SERVICES: "composer-agent,composer-executor,docker-socket-proxy,db,redis"
      COMPOSER_AGENT_RESTART_SERVICES: "web,celery,smtp-relay,caddy"
    volumes:
      - "${PWD}:${PWD}:ro"
      - dlux_runtime:/opt/dlux-runtime:rw
      - composer_agent_state:/var/lib/composer-agent:rw
      - composer_exec_sock:/run/composer-exec:rw
    depends_on:
      docker-socket-proxy:
        condition: service_started
      composer-executor:
        condition: service_started
    networks:
      - egress
      - docker_proxy
  # DjangoLux Composer agent end

volumes:
  postgres_data:
  redis_data:
  pgadmin_data:
  static:
  dlux_runtime:
  composer_agent_state:
  composer_exec_sock:
  caddy_data:
  caddy_config:

networks:
  # Published ingress only (caddy). Bridge so the host can reach the app and
  # Caddy can reach Let's Encrypt (ACME) for automatic HTTPS.
  frontend:
    driver: bridge
  # Outbound internet for the narrow set of services that need it: smtp-relay
  # (→ your SMTP provider), dlux-updater (→ PyPI), composer-agent (→ registry).
  # Separate from `frontend` so the internet-facing proxy shares no network with
  # them; nothing may sit on both.
  egress:
    driver: bridge
  # Inter-service traffic with NO internet access — db, redis, web, celery, …
  internal:
    internal: true
  # Isolated path from composer-agent to the docker-socket-proxy only.
  docker_proxy:
    internal: true
