# Changelog

## v0.6.6
- **Caddy Trusts Private-Range Front Proxies**: Added a global `servers { trusted_proxies static private_ranges }` block so `X-Forwarded-For`/`X-Forwarded-Proto` from a private front proxy are forwarded instead of replaced. Inert while Caddy terminates TLS directly; aligns with the django-lux 1.5.9 scaffold.

## v0.6.5
- **Manifest-Sourced Project Version**: `config/settings.py` now sets `DLUX_APP_VERSION = get_project_version(BASE_DIR)` (django-lux 1.5.3), reading the version from root `release-manifest.json` instead of the intermediate `VERSION`-file read. The root `VERSION` file stays the release gate's input for `tools/validate_project_release_manifest.py`, CI, and the image smoke test. Bumped `compose.dev.yml` `DLUX_BAKED_VERSION` to `1.5.3` to match the pinned wheel.
- **Private Resident-Updater Secrets Handoff**: Updated `start.sh` and `start.ps1` to pass the selected mode-`0600` env file and `COMPOSER_INHERITED_SECRET_KEYS` through Docker for Composer 1.1.15+, so recreating `composer-updater` provisions later image updates without host ACLs; also narrowed the tool self-update to `--update` as the sole argument so `--update <service>` reaches the app. Superseded scripts are archived under `.xpose/`.

## v0.6.4
- **Project Image Release Manifest**: Added root `release-manifest.json` schema-1 metadata and a reusable `tools/validate_project_release_manifest.py` release gate. Tagged builds now require the Git tag, root `VERSION`, manifest version/schema/field limits, and `debeski/Sales-CRM` release URL to agree, then stamp compact metadata into `org.dlux.project.release-manifest` on both the smoke-test and multi-arch images; Composer explicitly reads that label and `DLUX_APP_VERSION` exposes the running project version.
- **Terminal Image-Update Recovery**: Pinned `django-lux[updater]==1.4.9` so image handoffs consume Composer's token-matched terminal acknowledgement, fail closed after a 120-second no-start window, and lower the `dlux_runtime/state/maintenance` marker on failure. The Caddy-served `.proxy/maintenance.html` now probes the canonical same-origin `/` after a terminal failure and reloads the operator's original URL as soon as HTTPS application routing recovers, preventing a completed rollback from remaining stuck on the 503 page.
- **Reproducible DjangoLux Version Gate**: Replaced the open-ended `django-lux[updater]>=1.4.8` requirement with the exact v1.4.9 pin expected by `.github/workflows/release.yml`, ensuring `DLUX_BAKED_VERSION` is resolved and stamped into `org.switchlibya.dlux_baked_version` instead of silently producing an empty image label. Updated the development updater's stale `DLUX_BAKED_VERSION=1.4.7` to v1.4.9 and added a regression requiring both version sources to match.
- **Canonical `.proxy/` Proxy Layout**: Moved the reverse-proxy config into a single `.proxy/` directory — `Caddyfile` (was repo root) and `maintenance.html` (was `.nginx/`) now live in `.proxy/`, and the `caddy` service mounts `./.proxy/Caddyfile` and `./.proxy/maintenance.html`. The `.gitignore` `Caddyfile` rule is depth-agnostic so `.proxy/Caddyfile` stays untracked (env-specific, as before); `maintenance.html` moved as a tracked rename. README/OPERATIONS references updated; `docker compose config` (base + dev) and `caddy validate` pass.
- **Docker-Adjacent Container Hardening**: Added `security_opt: [no-new-privileges:true]` and `cap_drop: [ALL]` to both `docker-socket-proxy` and `composer-updater` in `compose.yml`. Neither needs Linux capabilities (the proxy's HAProxy binds `:2375` and reads the socket as owner-root; the updater only issues Docker API calls over TCP to the proxy), so dropping them shrinks the blast radius of the only two containers on the isolated `docker_proxy` network — the internet-facing app (`web`, on `internal` only) can reach neither the Docker socket path nor the internet. Also enabled `EVENTS=1` on `docker-socket-proxy` (so Compose can observe `up -d` progress) and set `COMPOSER_EXCLUDE_SERVICES=composer-updater,docker-socket-proxy` so the resident watcher never recreates itself or its gateway mid-update.
- **Caddy Stays Serving The Progress Page During Updates**: Changed the `caddy` service `depends_on: web` from `condition: service_healthy` + `restart: true` to `condition: service_started`, so composer recreating `web` no longer bounces Caddy — it keeps serving `/_update/*` polling and the maintenance progress page throughout (Caddy re-resolves `web`'s new IP per dial). Added a hybrid `/health` endpoint (proxies to Django `/health/` normally, returns proxy-local `200` while the dlux maintenance marker exists) and an always-200 `/_edge-alive` probe to the `Caddyfile`, so a front proxy's backend health check stays UP during planned maintenance.

## v0.6.3
- **DjangoLux Runtime Requirement Bump**: Bumped `requirements.txt` from `django-lux[updater]>=1.4.6` to `django-lux[updater]>=1.4.8` so the release bakes and installs the DjangoLux runtime fixes required by v0.4.8. No application schema, UI, or business-logic changes.

## v0.6.2
- **Homepage Builder Preview Language Isolation**: Changed `public_catalog.views._apply_public_language` so `/?preview=1&lang=<code>` staff preview iframe requests set a request-local `_public_preview_language` instead of writing `session['lang']` / `dlux_force_language_preview`; normal public header `?lang=` clicks still persist as before. `PublicLandingView` now resolves homepage content plus `CURRENT_LANG`, `CURRENT_DIR`, `DLUX_STRINGS`, `LANGUAGES`, and language-aware `APP_CONFIG` from that request-local preview language, while `PublicHomepageBuilderView` starts its edit-language toggle from `dlux.get_current_language_code(request)` so user display preferences win over the system default. Public/homepage-builder asset cache keys bumped to `?v=20260714l`; regression tests cover the English-user / Arabic-default session-bleed case.

## v0.6.1
- **Public Builder Options Tile Cleanup**: Removed the redundant `switch_pos.public_homepage` DLux Options tile and its Homepage Builder modal button, leaving homepage copy, sections, visuals, and the live toggle exclusively in `/staff/shop-builder/homepage/` while still persisting under `SystemSettings.extra_config['app']['switch_pos.public_homepage']` for backups/settings exports. Slimmed the `switch_pos.public_catalog` Options tile to shop identity, contact endpoints, and new-listing `show_price`/`show_availability` defaults; shop/homepage live toggles and `featured_limit` remain in the dedicated builders. Updated EN/AR tile descriptions, docs, and registry tests.
- **Homepage Builder Visual Studio**: Expanded `switch_pos.public_homepage` with normalized visual settings (`style_preset`, `hero_layout`, `hero_height`, `hero_focus`, `nav_treatment`, `card_treatment`, `section_density`, `background_treatment`, `motion_level`, `accent_secondary`) plus per-section `variant` persistence in `normalize_sections`. The builder now has a top Style panel with icon segmented controls, primary/secondary accent swatches, hero focus controls, tablet preview, and section layout selectors. `PublicLandingView` emits shell/hero/section class hooks plus `--public-accent-2`; `public_catalog.css` adds stronger signature/showroom/precision/editorial treatments, poster/center/mosaic/compact hero layouts, visibly distinct spec/minimal cards, denser compact sections, wider-spaced diagonal backgrounds, a light sparse connected electrical-grid background with broken circuit segments/nodes, denser multi-angle linework backgrounds, rail/chip/banner/contact variants, dark-theme fallbacks, and reduced-motion-safe styling. `style_preset` now materially changes page-level geometry/typography/surfaces (showroom, precision, editorial, signature) instead of only tweaking variables. `public_catalog.js` respects `motion_level=none` and `prefers-reduced-motion`; public assets bumped to `?v=20260714k`. Homepage listing cards now suppress item-detail links and the Quick view modal button when `shop_enabled` is off, so the homepage can stay live without exposing disabled shop/detail surfaces. Tests cover enum fallback, section variants, rendered hooks, cache keys, shop-disabled card affordances, visual CSS contracts, and preview `?lang=`.
- **Homepage Builder Preview Follows The Edited Language End-To-End**: The homepage builder's editing-language toggle now reloads the preview iframe with `?preview=1&lang=<code>` (the same session-based mechanism the public top-bar language toggle uses) instead of the content-only `?hl=` override. Previously only the localized homepage fields switched while the preview's **top bar** (project name from `APP_CONFIG.identity.display_name`), page direction (LTR/RTL) and DLux chrome stayed in the previous language; now the whole preview switches together, so the header matches the language being edited. `PublicLandingView` drops the `?hl=` branch and resolves purely through `dlux.get_current_language_code`. `homepage_builder.js` → `?v=20260714c`.
- **Homepage Builder Sidebar Label Translation**: Added the `public_catalog_staff_homepage_builder` DLux route-discovery key (EN "Homepage Builder" / AR), so the sidebar entry resolves per language instead of falling back to the humanized URL (which showed English in Arabic). Extended the discovery test to assert both builder labels in both languages.

## v0.6.0
- **Independent Homepage / Shop On-Off + Bare Coming-Soon**: Split the single `storefront_enabled` flag into **`homepage_enabled`** and **`shop_enabled`** (`public_catalog/settings.py`, with legacy migration mapping the old flag to both). The landing (`/`) gates on `homepage_enabled`, the shop/detail/item-modal on `shop_enabled` (`_StorefrontGateMixin.gate_key`, overridden on `PublicLandingView`), so turning one off no longer takes the other down — the coming-soon page shows only for the disabled surface. The catalog builder's live switch now drives `shop_enabled` ("Shop is live/offline"); the **homepage builder gained its own "Homepage is live" switch** (green pill in the header → `builder_settings`), and the Public catalog settings tile exposes both booleans. The coming-soon page is now **bare** — `public_base.html` hides the nav bar (logo/Shop/Contact/language toggle) and footer via a `public_bare` flag — and shows the **registered logo** (`APP_CONFIG.logo_url`) instead of the shop icon. `builder_settings` accepts both flags; `shop_builder.js`/`homepage_builder.js` post the respective flag. EN/AR strings (`public_catalog_homepage_enabled`/`_shop_enabled`, `builder_shop_*`, `hp_homepage_live/offline`); builder assets `?v=20260714a`/`b`. Tests updated to the split flags (independent-gate test) + bare-coming-soon assertion. Full suite 168 pass.
- **Homepage Builder: Multilingual Content, Hero Carousel, Layout Polish**: Reworked the homepage builder and public landing. **(1) Per-language content** — text fields (`homepage.LOCALIZED_KEYS`: hero kicker/title/subtitle/button, story body, every section kicker/heading) are now stored as `{lang_code: value}` dicts across DLux's discovered languages; `get_public_languages()`/`resolve_homepage(lang)`/`localize()` resolve the visitor's language with fallback to the default then any filled value, and legacy plain strings migrate into the default-language slot. **(2) Editing-language toggle** — the builder header carries a DLux-style `lang-toggle` (`.hpb-langtoggle`, mirrors the login toggle) that reveals each language's inputs (`data-lang`) and re-renders the live preview in that language via `?preview=1&hl=<code>` (staff-only, no session change); `homepage_save` parses `field__<lang>` inputs. **(3) Contact section fixed** — the `contact` landing section now always renders a working Contact button (opens the email modal) plus any configured contact links, instead of showing nothing when no phone/email is set. **(4) Hero image confined + blended** — replaced the single `.public-hero__image` with a `.public-hero__media`/`.public-hero__slide` container (`overflow:hidden`, `object-fit:cover`, width/height 100%) so tall images stay inside the hero, with a directional blend gradient (`::after`, RTL-aware) into the copy. **(5) Hero carousel** — when more than one featured highlight exists, the hero cross-fades through their images with autoplay + pill dot navigation (`public_catalog.js` `initHeroCarousel`, pause on hover). **(6) Wider, cleaner layout** — content max-width moved to a `--public-max: 1560px` var and the shell background lightened (whiter gradient, softer accent radials) so the sides read less empty/gray. **(7) Public language toggle** — the public storefront header now carries a `.public-lang` EN/AR toggle (mirrors DLux's login toggle); `_apply_public_language` (run in the shared public gate) writes `?lang=<code>` as a **forced language preview** (`session['lang']` + `dlux_force_language_preview`, the same mechanism DLux's own language picker uses — honored regardless of `allow_user_language_override`), and the landing resolves through `dlux.get_current_language_code`, so a click flips the page language/direction (LTR↔RTL) and the localized homepage content follows. Assets `public_catalog.css` → `?v=20260714b`, `public_catalog.js`/`homepage_builder.*` → `?v=20260714a`, `hp_edit_language` EN/AR. Tests updated to the localized shape + `.public-hero__slide` contract; +2 tests (localized resolution, public language switch). Full suite 167 pass.
- **Workspace Reset Button Removed**: Removed the one-click workspace dashboard reset control from `common/workspace_dashboard.html` and deleted its JS handler from `workspace_dashboard.js` (`?v=20260713a`). Workspace layout still persists under `Profile.preferences["app"]["switch_pos.workspace_dashboard.v1"]`, but when DLux app preferences are available the script no longer treats stale `localStorage` as authoritative; after DLux Reset Defaults clears `Profile.preferences`, the old browser copy is removed instead of being migrated back into app prefs on the next workspace load. Removed the unused `ui_reset_layout` strings and added tests asserting the reset control/handler are absent.

## v0.5.1
- **Public Homepage Builder + Settings**: New staff tool at `/staff/shop-builder/homepage/` (sidebar entry, `bi-easel2`) that composes the public landing page with a **live preview iframe** and autosave. Config lives in its own DLux app-settings namespace `switch_pos.public_homepage` (`public_catalog/homepage.py`: defaults, `normalize_sections`, `get/set_homepage_config`, `resolve_sections`) with a `register_app_settings` tile (hero copy, CTA, accent) registered alongside the catalog one. The builder edits: **hero** (kicker/title/subtitle with shop fallbacks, primary CTA label+link, Contact toggle, background mode — featured image / logo / custom upload / gradient — overlay-darkness slider, stats toggle), **sections** (reorderable via up/down arrows + enable toggles + per-section kicker/heading; Story section adds body + image), and an **accent** colour (swatch presets + picker + "theme default"). `landing.html` + `PublicLandingView` are now fully config/section-driven (ordered enabled sections: featured, categories, services, story, contact; category tiles derived from published product listings); the accent injects a `--public-accent` CSS var into `public_base.html`. `PublicHomepageBuilderView` + `homepage_save` reuse the `mutation_endpoint` wrapper (POST-only, silent 204 on GET, permission-gated `change_publiccataloglisting`) and `sidebar_exclude`; images upload through `default_storage`. Staff with `view_publiccataloglisting` can preview the storefront while it is **offline** via `?preview=1` (the builder iframe uses it) — anonymous visitors still get the 503 coming-soon page. New `homepage_builder.{html,css,js}` (`?v=20260712a`), `public_catalog.css` accent/hero-overlay/categories/story styles (`?v=20260712c`), EN/AR strings; +14 tests. Full suite 166 pass.

## v0.5.0
- **Public Catalog Builder Sidebar Label Translation**: Replaced the explicit `lazy_t("builder_title")` sidebar callback label with DLux's normal route-discovery key path by adding `public_catalog_staff_builder` in EN/AR. Discovery now resolves `public_catalog_staff:builder` as `Public Catalog Builder` / `منشئ المتجر العام` per requested language instead of evaluating a lazy label in the wrong language context.
- **Workspace Sidebar Label Translation**: Added the bare `workspace_dashboard` EN/AR translation key beside `common_workspace_dashboard` and covered DLux sidebar discovery for `common:workspace_dashboard`, so the workspace entry resolves to `Workspace` / `مساحة العمل` instead of falling back to a generated label.
- **Public Catalog Builder (Staff)**: New intuitive staff view at `/staff/shop-builder/` (sidebar entry, `catalog` group, `bi-shop-window`) that curates the public shop directly from live stock — no more Django-admin editing of `PublicCatalogListing`. `public_catalog/staff_views.py` lists every active `Product` and `Service` as a card (real listing or an unsaved transient one) sorted published→featured→name, with live counts (published / featured / available / in-stock). Each card has an inline **Publish** switch, a **Feature** star, an **Open public page** link, and a **Customize** modal (public title/summary/body, image override with preview + clear, installation/warranty notes, sort order, per-listing show-price/show-availability/show-when-unavailable). AJAX endpoints (`toggle-publish`, `update-listing` [multipart, get-or-create so customizing auto-drafts a listing], `reorder`, `settings`) are `POST`-only and gated by `public_catalog.change_publiccataloglisting`; the page needs `view_publiccataloglisting`. `shop_builder.js` handles publish/feature toggles, the customize modal, live search + kind/published filters, drag-to-reorder of published cards (grip handle → `reorder`), and a storefront on/off switch — persisting via `fetch` with toast confirmation and no full reloads. New `shop_builder.css` (theme-aware, reuses `.dlux-table-shell` + dlux vars). Featuring is refused unless published (server-enforced). The four write endpoints use a `mutation_endpoint` wrapper that mutates only on POST (permission-gated), returns silent **204** for passive GET/HEAD probes, and redirects browser-style GET navigation back to `/staff/shop-builder/` so a stale tab/login-next to `/staff/shop-builder/settings/` cannot show a `405`/`ERR_HTTP_RESPONSE_CODE_FAILURE`; they also set `sidebar_exclude = True` to stay out of DLux route discovery. Drag reorder is navigation-safe (grip-armed `draggable`, neutralised `dataTransfer`, `dragover`/`drop` `preventDefault`, `draggable="false"` on card image/public link), `shop_builder.js` `?v=20260712b`. EN/AR strings; builder discovery/navigation tests included. Public catalog remains squashed into one unreleased initial migration.
- **Public Storefront Settings + Coming-Soon Gate**: Extended the Public catalog system-settings tile (`register_app_settings`) with `storefront_enabled` (master on/off) and `featured_limit` (0–24 landing features), added to `PUBLIC_CATALOG_DEFAULTS` + a `set_public_catalog_config()` writer (via dlux `write_app_system_config`). The builder's inline storefront switch and the tile both drive it; when off, `PublicLandingView`/`PublicShopView`/`PublicItemDetailView`/item-modal return a themed `coming_soon.html` (HTTP 503) instead of the shop, and the landing hero/featured strip now honor `featured_limit`. EN/AR strings for all new labels; the builder's "Shop settings" button opens the tile modal (`dlux_app_settings_modal`) in-page.
- **Public Contact Modal And Sidebar Discovery Exclusions**: Removed the public Staff CTA from the landing/header and replaced it with a DLux dynamic-modal Contact action backed by `/contact/modal/`. Added `PublicContactMessage` with DB-unique `idempotency_key`, sender fields, honeypot, `source_path`, user-agent capture, email recipient/status/error timestamps, admin readout, and idempotent POST handling so a repeated browser submit creates one row and sends at most one email. Added `public_catalog/js/public_catalog.js` to open the contact dynamic modal as a full-screen Bootstrap dialog, `PublicContactForm`, modal template styling, and locmem email regression coverage. Marked public landing, shop, contact modal, and `/staff/` entry callbacks with `sidebar_exclude=True` so DLux sidebar discovery does not offer public/staff-entry routes as sidebar items.
- **Public Shop Storefront Makeover**: Reworked the anonymous public catalog templates into a fuller storefront while keeping the same `PublicCatalogListing` read-only projection and DLux modal JSON endpoint. `public_catalog.views` now provides `hero_listing`, public listing counts, and filtered result counts; `public_base.html`, `landing.html`, `shop.html`, `_listing_card.html`, `item_detail.html`, and `_item_modal.html` gained a sticky public header, image-led landing hero, catalog metrics, richer search/result header, availability overlays, page/quick-view actions, detail breadcrumbs, icon facts, and installation/warranty modal sections. Rebuilt `public_catalog/css/public_catalog.css` with DLux-compatible variables, light/rich-theme support, responsive storefront grids, 8px-radius public cards, contact modal styles, and cache key `?v=20260712b`. Added public layout contract tests that still assert no staff chrome, staff modal URLs, SKU/barcode/cost/markup, or exact stock counts leak publicly.
- **Default Timezone Africa/Tripoli**: Times rendered in UTC because `config/settings.py` defaulted `TIME_ZONE = os.getenv("TIME_ZONE", "UTC")` and no `TIME_ZONE` env var is set in `compose.yml`/`.secrets/.env`; with Django's default `USE_TZ=True`, datetimes stored UTC were displayed in the active (UTC) zone. dlux was not involved — it follows `timezone.get_current_timezone()`. Changed the default to `Africa/Tripoli` (UTC+2, no DST). Display-only: stored data stays UTC, so no migration. Override still available via the `TIME_ZONE` env var (also feeds `CELERY_TIMEZONE`).
- **Manual Stock Movement Variant Filtered By Product**: The Manual Movement modal's `variant` dropdown (`StockMovementForm`) listed **every** `ProductVariant` across all products — the field's `clean()` only rejected a product/variant mismatch after submit. Added a `VariantProductSelect` widget that tags each `<option>` with `data-product="<product_id>"` (map built from the field queryset in `__init__`) and a new `catalog/js/stock_movement.js` (loaded via `StockMovementListView.extra_scripts`, same event-delegation + `MutationObserver` pattern as `price_sync.js` so it works on the AJAX-loaded modal). On product change / modal load it shows only the selected product's variants (hides + disables the rest) and clears the selection if it no longer belongs; products with no variants leave only the blank option. +1 regression test.
- **Products Layout Selector Renders As DLux Toggle Control**: The per-user Products layout picker card and the superuser global-default tile both rendered as an unstyled vertical stack because dlux's `control: "selector"` default maps to the `dlux-choice-selector--card` variant, which has **no container layout rule** in `selectors.css` (only `--swatch`/`--chip`/`--toggle` define a grid). Both surfaces now use the segmented `--toggle` variant (the sidebar/table-density control): the option card renders the real `DluxChoiceSelectorWidget(variant="toggle", option_meta=<icons>)` from its `context_builder` in `catalog/dlux_options.py` (wrapped in `div_id_products_layout` so `selectors.js` applies the `--toggle` field frame; defensive import falls back to the `_products_layout_toggle.html` button group on dlux < 1.4.4), and the settings tile field adds `"variant": "toggle"` + per-choice `option_meta` icons + `"css_class": "col-12"` so the lone field spans the full row instead of `col-lg-6`.
- **Products Layout Card No Longer Reloads From Options**: `catalog/js/products_layout.js` `commitLayout` now takes an `onSuccess` callback — the Products-page header toggle still reloads (the list is already rendered), but the options-page card persists via `fetch` and shows a `window.showToast` confirmation (`options_products_layout_saved`) **without** reloading the options page. Card cache key bumped to `?v=20260711d`.
- **App-Settings Choice/Label Translations**: DjangoLux resolves settings-tile `title`/`description` per request (callable) but bakes field `label`s and `choices` at import; these called `t()` eagerly with no active request, freezing them to one language. Added `common.i18n.lazy_t` (`django.utils.functional.lazy(t, str)`) and switched the Products layout choices/labels and the entire Public catalog tile (`public_catalog/dlux_options.py`) to lazy strings + per-request `lambda` title/description, so they now follow the active language. Added `public_catalog/translations.py` (EN/AR, auto-merged by dlux) — the tile title, description and all field labels were previously hardcoded English — plus `options_products_layout_saved` (EN/AR).
- **Staff-Prefixed App Preference Saves**: Fixed the internal Products layout switcher and workspace dashboard persistence after the `/staff/` remount. `catalog/_products_layout_toggle.html` now emits a reversed `update_app_preference` URL template (`/staff/sys/api/preferences/app/__namespace__/`), `catalog/js/products_layout.js` posts directly to that staff-prefixed endpoint and reloads only after a successful save, and `common/js/workspace_dashboard.js` now prefers its reversed URL template before falling back to DjangoLux's global helper. Bumped affected static cache keys to `?v=20260711b` and added regressions for product layout URL rendering, scalar preference persistence, and JS contracts.
- **DLux-Owned Public/Staff Split And Public Catalog**: Remounted all internal routes under `/staff/` in `config.urls` (`dlux.urls`, `/staff/sys/...`, `/staff/accounts/...`, `/staff/workspace/`, `/staff/catalog/`, `/staff/sales/`, `/staff/finance/`, `/staff/app-modals/...`, `/staff/admin/`) with `LOGIN_URL="login"` so Django auth resolves `/staff/accounts/login/`. Added `public_catalog` with `PublicCatalogListing` (`Product` XOR `Service`, unique source constraints, public slug/copy/image override, publish/feature/sort flags, price/availability toggles, installation/warranty notes), anonymous public views for `/`, `/shop/`, `/shop/items/<slug>/`, and DLux dynamic-modal JSON at `/shop/items/<slug>/modal/`. Public templates use a lean DLux-styled shell and Bootstrap dynamic modal wiring without emitting internal `DLUX_STRINGS`/config JSON; public output redacts SKU/barcode/import cost/markup/exact stock and shows only broad availability bands plus variant labels. Added DLux public-root defaults (`home_url=/staff/workspace/`, `public_root=True`, `public_root_split_enabled=True`, `public_root_url=/`, hidden public titlebar/sidebar) via settings, `config.json`, and the squashed `public_catalog/0001`; fixed workspace dashboard app-preference POSTs to use a reversed `/staff/sys/api/preferences/app/<namespace>/` template instead of hardcoded `/sys/...`. Updated Caddy/Compose defaults so `switchlibya.ly`/`www.switchlibya.ly` proxy to Django while optional `erp.switchlibya.ly` redirects to `/staff/`; dev compose mounts `public_catalog`. Added split/catalog safety tests and updated README/docs for the new routes and future idempotent public write policy.

## v0.4.1
- **DjangoLux Runtime Requirement Bump**: Bumped `requirements.txt` from `django-lux[updater]>=1.4.3` to `django-lux[updater]>=1.4.4` so the release bakes and installs the DjangoLux runtime required by the v0.4.0 Products layout settings integration. No application schema, UI, or business-logic changes.

## v0.4.0
- **Sales Invoice POS Catalog Picker + Cart**: Replaced the wide 9-column sales-invoice line table with an "add to cart" flow. A new **catalog picker** panel (under customer details) has a Products/Services toggle, a live search, and a category filter, and renders a tile grid **client-side** from a richer `_catalog_map` (replacing `_price_map`) that surfaces **only sellable items** — products with in-stock `ProductVariant`s (or a stock-tracked variant-less product with stock, or a non-tracked product) and services (each with its uploaded image or a Bootstrap icon mapped from `service_type` via `SERVICE_TYPE_ICONS`, no model change). Variants are chosen **at add-time**: each in-stock colour/size is its own tile chip (swatch + qty), so every cart line binds to a specific variant with a known stock ceiling. Picked items drop into a compact, mobile-friendly **cart list** (`sales/_invoice_cart_row.html`) with an editable unit price + quantity, a live line total and subtotal, a per-line soft over-stock warning, and a remove action; a "Custom line" button preserves free-text lines. `InvoiceItemForm` now renders `product/service/variant/color/size/kind/description` as hidden inputs (the picker writes them) with only price/quantity/description editable, and `InvoiceItemFormSet` uses `extra=0` so a new invoice starts with an empty cart. The proven server save path (`_apply_item_price`, per-variant issue/cancel stock guard) is unchanged. New `sales/static/sales/{css/invoice_editor.css,js/invoice_editor.js}` (reusing the themed `.dlux-table-shell` surface), `categories` context + EN/AR strings. Two tests updated to the new `_catalog_map` shape + cart error markup; +4 new tests (stock filtering, service icons, picker render, product-variant/service save via the cart POST). Full suite 134 pass.
- **Per-User Products Layout — Table / Grid / Light**: The Products page can now render three ways, chosen per user. The choice lives in `Profile.preferences['app']['switch_pos.products_layout']` (the reserved DjangoLux app-preference store, same as the workspace dashboard) and is read server-side by `catalog.product_layouts.get_products_layout()` (fail-safe to `table`). `ProductListView` branches on it: **table** keeps the full `ProductTable`; **light** swaps to a new minimal `ProductLightTable` (name · price · stock · active only, everything else in the record's detail modal) via `get_table_class()`; **grid** renders a new `catalog/product_grid.html` — a store-style responsive card grid where each product card shows image, LYD price, stock, and in-stock colour/size variants (`Product.variant_summary_html()`), with an Expand action that opens the same dlux detail modal the table rows use (`scoped_modal_manager … ?action=view`). Grid reuses the table's filtered+paginated rows and querystring, and every layout reuses DjangoLux's themed `.dlux-table-shell` surface. Resolution order is **per-user override → global admin default → `table`**. The global default is a superuser **settings tile** registered with dlux 1.4.4's new `register_app_settings` (`catalog/dlux_options.py`) — a `choice`/selector field saved to `SystemSettings.extra_config['app']['switch_pos.products_layout']['default_layout']` and read by `get_default_products_layout()` via `dlux.utils.get_app_system_config`. Per-user switching stays a `register_card` picker + inline header toggle sharing one component (`catalog/_products_layout_toggle.html`) and script (`catalog/js/products_layout.js`, persists via the global `window.updateAppPreference` then reloads). Added a `{% block list_body %}` to the shared `templates/common/scoped_list.html` so alternate layouts can replace the table body while keeping the header, filter and modal-CRUD wiring; new `product_list.html` (adds the toggle) and `product_grid.html` extend it. EN/AR strings for the switcher + admin default. **Requires dlux ≥ 1.4.4** (`requirements.txt` bumped from `>=1.4.3`); registrations import defensively so an older runtime still gets the per-user card. +13 tests (`catalog/tests/test_products_layout.py`).
- **Stock-Bearing Color/Size Variants**: Replaced the overwrite-prone product-level variant workflow with `catalog.ProductVariant(product,color,size,stock_qty)` buckets linked from `PurchaseInvoiceLine.variant`, `StockMovement.variant`, and `InvoiceItem.variant` (`catalog/0006`, `sales/0007`). Opening Stock and Purchase Invoice rows now create/reuse a matching variant and post variant-linked Stock In movements without rewriting `Product.color/size`; `StockMovement.save()` updates both aggregate `Product.stock_qty` and the selected `ProductVariant.stock_qty`. Sales invoice issue/cancel now aggregate stock demand per variant, so blue stock cannot satisfy an orange shortage for the same product, while legacy/no-variant lines still use aggregate product stock. Product detail/list views render available variant color swatches with quantities, Sales invoice lines use swatch-driven variant selection with visible invalid-field feedback for missing quantities, and migrations backfill variants from historical purchase-line/product snapshots with existing invoice items linked to matching variants.
- **Rich Cross-Module Demo Seeder**: Expanded `sales/management/commands/seed_demo.py` from a small sales fixture into a full operating dataset: five `demo_*` users (`demo12345` local-demo password), five `ExchangeRate` history rows, four `Supplier` records, eight `Category` records, 24 `Product` records with barcode/color/size metadata and opening `StockMovement` rows, 11 `Service` records, 12 `Customer` records, four posted `PurchaseInvoice` documents with `PurchaseInvoiceLine` snapshots and linked stock-in movements, 18 `Invoice` documents across draft/issued/partial/paid/cancelled, cash/bank/cheque `CashDeposit` batches linked through `Payment.deposit`, 10 `Delivery` rows, posted/draft/void `Expense` rows, `StaffAccount` + `StaffLedgerEntry` samples, and open/applied `StockTake` sheets. `--reset` now clears the new dependent tables in safe order; reruns still top up reference rows and skip duplicate operational documents once invoices exist. `docs/OPERATIONS.md` documents the seed shape and command usage.
- **Business Math Stress Tests**: Added high-precision regression coverage in `sales/tests/test_business_math.py` and `finance/tests/test_finance.py`: exact invoice line rounding plus percentage-discount math (`558.32` subtotal → `41.87` discount → `516.45` total), fiscal-year P&L chaining revenue/cash/COGS/gross margin/expenses/net profit/receivables/current inventory value, duplicate-product aggregate stock guards with rollback and cancel restoration, stock-take signed variance value and applied adjustment math, and a full staff-ledger signed-entry matrix proving posted positive/negative entry types affect balance while pending/disputed/void rows do not. `sales.tests.test_sales.SeedDemoCommandTests` now asserts the expanded fixture counts and deposit-linked payments.

## v0.3.0
- **Report/Dashboard & Form Surfaces Reuse DjangoLux Themed Classes**: Reskinned the four hand-built views that extend `dlux/base.html` directly — `sales.DashboardView`, `SalesReportView`, `FinancialReportView`, and `catalog.InventoryValuationView` — which previously used raw Bootstrap `card border-0 shadow-sm` KPI tiles and `<table class="table"><thead class="table-light">` and so read as stark white slabs against the dlux light theme (and didn't follow the theme at all). Rather than inventing project CSS, every surface now **reuses DjangoLux's own globally-loaded, fully-themed `.dlux-table-shell`** (its `--dlux-table-*` variables are overridden by all 12 shipped themes — light/blue/gold/green/mono/red + aether/dark/gothic/neon/prism/retro — so it themes everywhere via variables, no per-theme rules to maintain): data tables use the native `.dlux-table-shell > .dlux-table-scroll > table.dlux-data-table.table` markup (`.dlux-table-head/body/foot`), and the KPI stat tiles / list panels reuse `.dlux-table-shell` as a padded glass panel (`p-3` for tiles; `border-bottom`/`border-top` + `list-group-flush` items with `bg-transparent` for the deliveries/low-stock panels). No view/Python changes and no DluxTable conversion. (Superseded the first-pass attempt that used a bespoke `common/css/report_surfaces.css` / `.rpt-card` with only five hand-written theme overrides — retired to `.xpose/` — which looked right on the default theme but broke on the others.) Also removed the solid white `card border-0 shadow-sm` wrapper around the **customer details** (sales invoice form) and **supplier details** (purchase invoice form) sections — now borderless `<section>`s with an `<h2>` heading so the fields sit on the themed page background. Verified: `check` clean, 96 sales/catalog/common tests pass, and a render probe confirmed all four report/dashboard templates return 200 with `.dlux-table-shell` and no remaining raw cards or invented classes.
- **Browser Tab Titles For Shared Scoped List Pages**: Added a `{% block title %}{{ page_title }}{% endblock %}` to the shared `templates/common/scoped_list.html`. DjangoLux `base.html` renders `<title><app> | {% block title %}…{% endblock %}</title>`, but the generic scoped list template never filled that block, so every page driven solely by `common.ScopedListView` (Stock Takes, Stock Movements, Purchase Invoices, …) rendered a blank tab title while templates that declare their own title (e.g. `sales/invoice_list.html`) did not. The view already computes `page_title` (order: `page_title_key` → `page_title` → `model_<name>` string → `verbose_name_plural`), so all scoped lists now inherit a real, bilingual tab title with no per-model template change.
- **Workspace Dashboard Aether/Rich Theme Tile Fix**: Fixed `common/static/common/css/workspace_dashboard.css` so `.workspace-tile`, `.workspace-drawer`, `.workspace-empty`, `.workspace-toggle`, `.workspace-icon-btn`, `.tile-tool`, and `.action-chip` all include `:root.theme-aether` in the same dark/rich-theme surface path already used for dark/retro/gothic/prism/neon, preventing aether from rendering pearl text on the light options-card fallback. Tile tone accents now route through `--ws-*-rgb` variables with brighter dark-theme overrides (`green=#4ade80`, `blue=#60a5fa`, `amber=#fbbf24`, etc.) so metric bars, icon badges, and row status accents stay legible on aether/prism/neon. Bumped the stylesheet cache key in `common/templates/common/workspace_dashboard.html` to `?v=20260710d` and added `common.tests.test_workspace_dashboard` coverage for the aether selectors and tone-variable path; dashboard HTML/JS data contracts remain unchanged.
- **Workspace Dashboard Adopts DjangoLux Options-Card Look**: Restyled the `common` workspace dashboard tiles (`common/static/common/css/workspace_dashboard.css`, cache-bust `?v=20260710b`) to match the DjangoLux Options page cards (`dlux/main/css/options.css` `.dlux-options-panel`): 1.4rem rounded glass panels with the `linear-gradient(160deg, rgba(255,255,255,.94), rgba(primal,.055))` surface, inset-highlight + soft drop shadow, a thin 4px gradient accent bar via `::before` (now tone-coloured per tile through new `--tile-accent-rgb` triples instead of the old thick `border-top`), a tinted rounded icon badge on each tile/action heading, dashed grip-handle styling on the drag tool, options-style quick-action chips + customize drawer, and the same `:root.theme-dark/retro/gothic/prism/neon` panel overrides. Also fixed the metric value/currency layout (`.metric-value`): the number + Latin unit now render as one baseline-aligned `inline-flex` LTR pair with a proper gap, parked at the card inline-start via `text-align: start`, so they no longer collide at the start edge under Arabic RTL (cache-bust `?v=20260710c`). Purely presentational — the tile HTML/JS contract (`data-widget-id`/`data-size`/`data-default-size`, `.is-dragging`/`.drag-target`, `[data-dashboard-*]`) is unchanged, so reorder/hide/resize/reset and per-user layout persistence keep working; `check` clean + 6 common tests pass.
- **Model Plural Label Keys (AR/EN)**: Added the plural `models_<model_name>` translation key alongside every existing singular `model_<model_name>` in `finance`, `sales`, and `catalog` translations, in both `en` and `ar`. DjangoLux resolves model labels via `resolve_model_label` in order plural→singular→raw `verbose_name_plural`, so list/sidebar/section-manager surfaces previously fell back to the singular key (or raw English plural). Covers `exchangerate/cashdeposit/expensecategory/expense/staffaccount/staffledgerentry` (finance), `customer/invoice/payment/delivery` (sales), and `category/supplier/product/service/stockmovement/purchaseinvoice/purchaseinvoiceline/stocktake` (catalog); the two pre-existing finance `ar` plurals (`cashdeposit`, `expensecategory`) gained their missing `en` counterparts. Singular/plural key parity now verified across all three apps in both languages.
- **Finance Expenses And Staff Credit Ledger**: Added finance-side operating expense tracking and per-user staff credit accounts. New `ExpenseCategory` and `Expense` models (`finance/0003`) store posted/draft/void LYD operating costs with optional receipt/photo/PDF attachment via the dlux archive/scanner widget; posted expenses now feed `sales.reports.build_financial_report()` as `operating_expenses` and `net_profit`. New `StaffAccount` and append-only `StaffLedgerEntry` models track service/commission earnings, reimbursements, advances, loans, cash/item check-outs, and payments to/from staff with signed posted balances, pending user-confirmation rows, dlux persistent notifications (`recipients=[user]`, `action=confirm_staff_entry`), Confirm/Dispute/Void actions, row ownership via `OWNER_FIELDS`, manager `view_all_*`/`resolve_staffledgerentry` permissions, workspace tiles/quick actions, role-seed grants for managers/reps/couriers, AR/EN strings, docs, and focused finance/report tests.
- **Product Variant Attributes On Stock Intake And Sales Lines**: Added nullable/blankable `Product.color` and flexible `Product.size` (`Size / Spec`) fields plus matching historical snapshots on `PurchaseInvoiceLine` in `catalog/0005` and `InvoiceItem.color/size` in `sales/0006`. Product create/edit stays identity/pricing-focused while Product list/detail surfaces available color and size/spec; Opening Stock and Purchase Invoice line grids set/update variants at intake using a compact 15-choice swatch popover (`black`, `gray`, `white`, `red`, `blue`, `green`, `yellow`, `orange`, `purple`, `pink`, `brown`, `beige`, `navy`, `gold`, `teal`) plus a free-form size/spec cell for size, capacity, measurements, or product-specific descriptors. Existing-product autofill includes unit/import cost/margin/USD selling price/barcode/color/size, sales invoice product lines now expose optional color/size selectors populated from the selected product, purchase invoices now start with one editable row instead of 9 empty rows, and purchase/sales detail + print views show snapped variant metadata. Product list/search/admin, AR/EN strings, docs, and focused tests were updated for palette constraints, flexible size/spec handling, autofill payloads, purchase/sales line snapshots, one-row formset initialization, and catalog/sales translation parity.

## v0.2.4
- **Workspace Dashboard And Sales Overview Split**: Added a project-wide `/workspace/` dashboard through `common.WorkspaceDashboardView` and `common.urls`, while the existing `/sales/dashboard/` surface is now labeled **Sales Overview**. The workspace builds permission-gated, scope-filtered, ownership-aware tiles across `finance`, `sales`, `delivery`, and `catalog` (`Invoice`, `Payment`, `Customer`, `Delivery`, `Product`, `PurchaseInvoice`, `Supplier`, `StockMovement`, `StockTake`, `CashDeposit`), includes quick actions filtered by the exact underlying permissions, and stores each user's tile order/hidden state/size under DjangoLux 1.4.2's reserved `Profile.preferences["app"]["switch_pos.workspace_dashboard.v1"]` namespace with browser `localStorage` retained only as a fallback/migration layer. The UI is a dlux-theme-aware tile grid with hide/show drawer, reset, resize cycling, drag reorder, Bootstrap icon controls, AR/EN strings with translation parity coverage, and +6 dashboard tests.

## v0.2.3
- **Purchase Invoices And Supplier-led Stock Intake**: Added catalog-side procurement documents for inbound stock: new `Supplier`, `PurchaseInvoice`, and `PurchaseInvoiceLine` models plus `catalog/0004`, with a full-page Purchase Invoice form at `/catalog/purchase-invoices/new/`, supplier search-and-add combobox, product search-and-add line grid reusing the Opening Stock product map (`cost_usd`, `markup_percent`, `price_usd`, `price_lyd_override`, `category`, `unit`, `barcode`) and row-scoped price sync, and one posted `StockMovement(type=in, reference=PINV-..., purchase_invoice=...)` per line. Purchase invoices have list/detail/print surfaces, Dlux row context-menu actions with print `target="_blank"`, official System Settings logo on the print template, and an optional scan/photo/PDF `attachment` (`upload_to purchase_invoices/`) using dlux's rich archive file widget. The mistaken sales `Invoice.attachment` field was removed in `sales/0005_payment_receipts.py`; sales invoice forms/views/templates no longer bind or render attachments. Stock Movements now exposes separate controls for one-time Opening Stock, Add Stock (Purchase Invoice), and Manual Movement; once Opening Stock exists (`reference="OPENING"`), the action switches to a read-only grouped Opening Stock view. `seed_roles` grants Sales Manager supplier/purchase-invoice permissions; docs and AR/EN strings updated; +9 procurement/opening-stock tests.
- **Opening Stock Existing-product Autofill Fix**: Fixed the bulk Opening Stock row autofill guard so selecting an existing `Product` overwrites untouched row defaults (`0.00` cost/markup/USD and default `piece` unit) with the product-map values for `cost_usd`, `markup_percent`, `price_usd`, `price_lyd_override`, `category`, `unit`, and `barcode`, while fields marked as manually edited by the user remain preserved.
- **Manual LYD Price Sync On Cost Changes**: Fixed `catalog/js/price_sync.js` so Product forms treat a filled `price_lyd_override` as the active price source when `cost_usd` changes: the LYD override stays fixed, `price_usd` is recomputed from `LYD / current_rate`, and `markup_percent` is recalculated from the new cost. Blank LYD override behavior is unchanged (`cost_usd` changes keep markup fixed and recompute USD). The script now supports row-scoped pricing groups via `data-price-sync-row` and is loaded by the Opening Stock bulk grid, so each opening-stock row gets the same pricing behavior without cross-row updates. The `ScopedListView.extra_scripts` include now appends `?v=20260709a` so the browser picks up the corrected modal script despite long-lived static caching.
- **Opening Stock — one-time bulk inventory intake (رصيد افتتاحي)**: A first-setup shortcut to load a whole shop's existing shelf stock in one pass instead of adding each product then reconstructing history invoice-by-invoice. Deliberately **not** its own model/document — it's a **child of the stock ledger**: a trigger button on the Stock Movements page (`/catalog/stock-movements/`, gated by `add_product` + `add_stockmovement`) opens a full-page grid (`OpeningStockEditorView` at `/catalog/stock-movements/opening-stock/`) that reuses the invoice line-item pattern — a plain `OpeningStockLineFormSet` (`formset_factory`, add-row JS) with a per-row **new-or-existing product combobox** (`<datalist>` name input + hidden product id; JS fills it on match and autofills cost/markup/price/category/unit/barcode from a `product_map` JSON, blank = a brand-new item). Submitting (behind a translated confirm describing exactly what will happen) runs one transaction: each row create-or-reuses its `Product`, corrects its pricing when the admin edited those cells (the "mix" behaviour), and posts one **Stock In `StockMovement`** (`reason="Opening balance"`, `reference="OPENING"`) for the storage quantity — never writing `Product.stock_qty` directly, so the ledger stays authoritative; zero-qty rows reprice without a movement, blank rows are dropped. The posted movements remain the authoritative record, visible/filterable in Stock Movements and grouped in the read-only Opening Stock view after the one-time intake is used. +4 tests.
- **Individual Payment Receipts (إيصال قبض)**: `Payment` now has a durable unique `receipt_number` (`RCT-000001` style; migration `sales/0005` backfills existing rows), searchable from `PaymentFilter` and linked from both `PaymentTable` and each invoice's payments table. Added a print-ready receipt route at `/sales/payments/<id>/receipt/`, gated by `sales.view_payment` plus existing `Payment` row ownership, showing customer/invoice, amount, method, paid time, receiving user, optional cash-deposit reference, previous-paid total, and balance after that specific receipt. The receipt and invoice print headers now both use the official DjangoLux System Settings logo via `APP_CONFIG.logo_url`. Added EN/AR strings and +5 payment/visibility tests.
- **Dlux Invoice And Payment Row Actions**: Switched `InvoiceTable` and `PaymentTable` to standard DjangoLux row context menus for full-page actions: invoice rows expose view, print, draft edit, issue, and cancel form actions; payment rows expose view-invoice and print-receipt actions. Invoice numbers, payment receipt numbers, and invoice columns are now display values instead of hidden navigation triggers, invoice-detail sub-payment rows now expose `Print Receipt` through inline `data-dlux-actions`, and print actions use DjangoLux context-menu `target: "_blank"` URL actions so receipts/invoices open in a new tab.

## v0.2.2
- **Fiscal-year financial report**: New whole-store P&L at `/sales/financial/` (gated by a new `sales.view_financial_report` permission; **not** row-scoped — COGS/margins/capital-in-stock are owner-sensitive). A fiscal year is the calendar year (Libya); a year picker defaults to the current year and spans back to the first invoice. Reports **period** figures — revenue (live invoices in the window), **COGS**, gross profit + margin, cash collected — and **current-snapshot** figures (outstanding receivables, inventory value via the same Σ(stock × cost)), plus a revenue-by-month table. `build_financial_report()` in `sales/reports.py`; `seed_roles` grants the Sales Manager the perm; a Financial button appears on the dashboard for holders. Migration `sales/0004`; +6 tests.
- **Exact COGS — frozen unit cost per invoice line**: `InvoiceItem` gains `unit_cost_usd`, the product's unit cost **frozen at the time of sale** (set by the editor's `_apply_item_price` and, as a fallback for any other path, by `InvoiceItem.save()`), mirroring how selling price/rate are already frozen. The financial report's COGS now uses this frozen cost × the invoice's frozen rate, so it's exact and immune to later cost changes — with a `Coalesce` fallback to the product's current cost for lines created before this feature. Migration `sales/0004`; the "estimate" caveat is dropped.
- **Optional invoice attachment (scan/photo)**: `Invoice` gains an optional `attachment` `FileField` (`upload_to invoices/`) for a scan or photo of the signed/physical invoice. The invoice editor renders it with **dlux's rich archive file widget** (`_build_archive_file_widget`, `show_scan=True`) — drag-drop + preview + **Upload** (camera-or-gallery on a phone via `accept="image/*,application/pdf"`) + **Scan** (desktop TWAIN via ScanLink) — laid out full-width in the header grid. The editor `<form>` gained `enctype="multipart/form-data"` and the create/update views now pass `request.FILES`; the invoice detail page shows a paperclip link to the file. Migration `sales/0004`; +1 test.
- **Stock take (physical inventory count) + inventory valuation**: New `catalog.StockTake` + `StockTakeLine` models for a physical count session (جرد). A full-page count sheet (`/catalog/stock-takes/new/`) lists every active stock-tracked product with a "counted" input and **snapshots** the system quantity per line; the take opens in `open` status. Its detail page shows a **variance report** (system vs counted, signed variance, LYD value of each discrepancy via variance × unit cost) and — for holders of the new `apply_stocktake` permission — an **Apply** button that posts one **Adjustment `StockMovement`** per real discrepancy on a tracked product, bringing `stock_qty` to the counted figure (reusing the existing signed-adjustment ledger), then locks the take as `applied` (re-apply refused). Also a read-only **Inventory Valuation** report (`/catalog/valuation/`) = Σ(stock × unit cost) in USD and LYD, gated by `view_inventory_valuation`. Wiring: `StockTakeListView` (list, with New-count + Valuation header buttons via a new overridable `list_actions` block in `scoped_list.html`), `StockTakeTable`/`StockTakeFilter`, routes, and EN/AR strings. Not row-scoped (inventory is a management concern, gated purely by permission); `seed_roles` grants the Sales Manager the stock-take + valuation perms. Migration `catalog/0003`; +7 tests.
- **Multi-column forms (less scrolling)**: The dlux `DynamicModalManagerView` (our `scoped_modal_manager`) renders `{% crispy form %}` but attaches **no** helper/layout, so every field stacked full-width (the 2-per-row auto-layout lives only in dlux's unused section-manager view). New `common.forms.build_grid_helper(form, rows)` gives a form a crispy `FormHelper` + `Layout` that groups short fields onto shared Bootstrap rows (2–3 across; textareas/image full-width), which the template honors. It adds no submit input, so dlux's icon Save/Cancel buttons still render; drops permission-gated absent fields; emits hidden fields as bare inputs; and appends any unlisted field full-width so nothing is ever lost. Applied to the modal forms (Category/Product/Service/StockMovement, Customer/Delivery, ExchangeRate/CashDeposit) **and the full-page invoice header** — whose template was switched from the `{{ form|crispy }}` filter (which silently drops `form.helper`) to the `{% crispy form %}` tag; the items formset is untouched.
- **`track_stock` toggle description**: added `help_product_track_stock` (EN/AR) so the Product form's Track Stock switch carries an explanatory line like its neighbours ("When on, this item's quantity is managed by the stock ledger and low-stock alerts apply. Turn off for made-to-order or non-inventory goods.").
- **Product & Service images**: `catalog.Product` and `catalog.Service` gain an optional `image` `ImageField` (`upload_to catalog/products|services/`). The form field uses **dlux's rich archive file widget** (`_build_archive_file_widget`, template `dlux/forms/file_input.html`) — a drag-and-drop card with thumbnail preview and upload button — rather than the plain input, with `accept="image/*"` preserved on the underlying input so a phone still offers camera-or-gallery (dlux's own `show_scan` button is a desktop TWAIN scanner, left off). The widget is safe inside the AJAX modal: `file_field.js` loads globally from `dlux/base.html` and a `MutationObserver` initializes the card when the modal injects it. Shown as a 38px thumbnail in the list tables (neutral placeholder icon when empty) and a larger thumbnail in the detail modal — the latter via a new `is_html` flag on the project's `extra_detail_fields` override, fed by a shared `_image_detail_row()` helper on both models. Verified the dlux modal submits `new FormData(form)` (multipart) and the manager POST passes `request.FILES`, so uploads work end-to-end. `Pillow` pinned in `requirements.txt`. Media is served by Caddy in prod and, new, by Django's `static()` under `DEBUG` for local `runserver`; same-origin `/media/` is already allowed by CSP `img-src SELF`. +5 tests (`catalog/tests/test_images.py`); migration `catalog/0003`.

## v0.2.1
- **Live image-update progress page**: During an image update the app is recreated and briefly unreachable — previously the operator saw a bare "please wait"/404 with no feedback. `.nginx/maintenance.html` is now a self-contained live page: it polls `/_update/status.json` (composer's `deploy-status.json`) and `/_update/log.txt` (composer's `deploy-log.txt`) — both served by Caddy directly from the `dlux_runtime:ro` volume (Caddy stays up while `web` recreates) — and shows a real progress bar (pulling → recreating → migrating → ready), a live console, and the failure reason on error. On completion it reloads in place (returning the operator exactly where they were). A stale-`ready` guard only completes after observing in-progress this session (or a 20s timeout).
- **Caddyfile `/_update/*` routes**: added `/_update/status.json` and `/_update/log.txt` file routes *above* the `@maintenance` short-circuit, with `Cache-Control: no-store`, so the progress page can poll live status even while the app is down.
- **Faster update pickup + console**: `composer-updater` pinned to `debeski/composer:v1.1.7` (writes the console log) and `--interval` dropped `30`→`5s` so a queued update starts promptly (shrinks the window where the page shows a stale status).

## v0.2.0
- **Per-employee row-level visibility (multi-user sales model)**: On top of dlux's model-level permissions, records are now filtered by *ownership* so a sales rep sees only their own work, a manager sees the whole store, and a delivery courier sees only their jobs. New `common/access.py` centralizes it: each owned model declares an `OWNER_FIELDS` tuple (ORM lookups pointing at a user) and gains a `view_all_<model>` permission; `apply_ownership(qs, user)` returns the full queryset for a superuser or a holder of `view_all_<model>`, otherwise `filter(OR of owner lookups)`. A model without `OWNER_FIELDS` (the shared product catalog, exchange rates) is never row-filtered. Filtering runs at the **read choke points** (not the model manager, so `invoice.payments.all()` recalcs stay whole): `ScopedListView.get_queryset`, the invoice detail/print/editor/issue/cancel/payment lookups (`_visible_invoices`), and `build_sales_report`.
- **Assignable salesperson on invoices**: `Invoice` gains a `salesperson` FK (defaults to the creating user via `Invoice.save` / the editor) and `OWNER_FIELDS = (salesperson, created_by)`, so ownership survives handoff. The editor's salesperson picker is manager-only (new `assign_salesperson` permission); a rep can't reassign a sale away from themselves. New `view_all_invoice` lifts row-scoping for managers. Added a Salesperson column to `InvoiceTable`.
- **Private customer books**: `Customer` is now owned (`OWNER_FIELDS = (created_by,)`, `view_all_customer`) — a rep only sees/binds/searches their own customers (the invoice editor's customer datalist and `_sync_customer` find-or-create are ownership-filtered). `Payment` (`created_by` OR `invoice__salesperson`, `view_all_payment`) and `finance.CashDeposit` (`created_by`, `view_all_cashdeposit`) are owned too.
- **Delivery board (new `sales.Delivery` model)**: A courier-facing delivery job — optional `invoice` link, `assigned_to` courier, snapshotted recipient/phone/address, a six-state `status` (pending→assigned→out→delivered/failed/cancelled, auto-advances to *assigned* when a courier is set and stamps `delivered_at` on delivery), and `scheduled_date`. `OWNER_FIELDS = (assigned_to, created_by)` so a courier sees only their assigned jobs; `view_all_delivery` + `assign_delivery` for dispatchers. Full surface: `DeliveryListView` (modal CRUD), `DeliveryTable`, `DeliveryFilter`, `DeliveryForm` (courier picker gated by `assign_delivery`), `/sales/deliveries/` route, and EN/AR strings. The auto-discovered sidebar entry appears only for users with `view_delivery`.
- **Dynamic-modal ownership guard**: The project's `scoped_modal_manager`/`scoped_modal_delete` routes resolve objects through dlux's module-level `_scope_filtered_modal_queryset`; `common.access.install_modal_ownership_patch` wraps that single helper (all three edit/view/delete call sites route through it) so a rep can't open another rep's private Customer/Payment/Delivery by guessing its id. Installed on the first `request_started` (not app-init) to avoid a startup DB query from importing `dlux.views`.
- **Role-aware dashboard**: `DashboardView` was `LoginRequiredMixin`-only and showed **global** today/month sales, outstanding balances and recent invoices to *anyone logged in* (a leak). Every panel is now both permission-gated and row-scoped: sales figures/recent-invoices show only under `view_invoice` and cover only the viewer's own sales (whole store for a manager); a new **Deliveries** panel shows under `view_delivery`; low-stock shows only under `catalog.view_product`. So a delivery courier's home page shows deliveries — never sales.
- **Roles command**: `seed_roles` rewritten from one "Sales Staff" group into three — **Sales Manager** (all view_all_* + assign_* + reports + catalog/rate + confirm deposits), **Sales Representative** (own data only, no view_all), **Delivery Courier** (own deliveries + cash collection, no invoices/reports). Run `python manage.py seed_roles`; assign each user one group, keep the owner a superuser.
- **Tests**: +15 (`sales/tests/test_visibility.py`) covering the ownership helper (own/assigned/view_all/superuser/private-customer), list + detail choke points (rep vs manager, cross-rep 404), the modal-lookup patch, delivery assignee scoping + address snapshot, the salesperson default, and the role-aware dashboard context. 46 tests pass. Migrations: `sales/0003` (salesperson + Delivery + Meta perms), `finance/0002` (view_all_cashdeposit).

## v0.1.5
- **Baked DjangoLux bumped to 1.3.2**: `requirements.txt` now pins `django-lux[updater]==1.3.2` (was 1.2.14). The VPS deployment had already inline-updated to 1.3.1, so a rebuilt image baking an older 1.2.x would be **older than the active runtime** and composer's preflight version gate would (correctly) refuse to deploy it. Pinning ≥ the deployed version keeps the baked label (`org.switchlibya.dlux_baked_version`) at or above `active.json`, and picks up the dlux 1.3.2 registry-driven image-update fixes.
- **Fixed `composer-updater --check-image` target**: the registry availability check pointed at the wrong default image (`debeski/decrees:latest`, copied from the decrees template) — corrected to `${WEB_IMAGE:-debeski/sales:latest}` so composer polls this project's own image for a newer digest.

## v0.1.4
- **Baked DjangoLux version label**: `Dockerfile` now takes an `ARG DLUX_BAKED_VERSION` and sets `LABEL org.switchlibya.dlux_baked_version`; `.github/workflows/release.yml` derives it from the pinned `django-lux[updater]==<ver>` in `requirements.txt` (a new "Resolve baked DjangoLux version" step) and passes it via `build-args` to both the smoke-test build and the multi-arch push, so the label ships on the released image. Lets composer's preflight version gate compare a target image's baked framework version against the deployment's active runtime version. Empty locally = gate skipped.
- **Composer-as-updater services**: Added `composer-updater` (image `debeski/composer:1.1.5`, `command: watch`) — a resident, trigger-driven image updater watching `/opt/dlux-runtime/state/image-update-request.json` on the shared `dlux_runtime` volume; on request it runs `composer -uo` (pull → version gate → recreate → health → `post_start` migrator) against the host daemon and publishes `deploy-status.json`. Added `docker-socket-proxy` (tecnativa) as a least-privilege Docker API gateway (containers/images/networks/volumes/exec/POST/info/ping/version only) so the updater never mounts the raw `/var/run/docker.sock`. Version-gate wiring via `COMPOSER_VERSION_LABEL=org.switchlibya.dlux_baked_version` + `COMPOSER_ACTIVE_VERSION_FILE=/opt/dlux-runtime/state/active.json`. The updater is pinned (not `:latest`) to avoid recreating itself mid-update and mounts the project at the host path (`${PWD}:${PWD}`) so compose-in-container resolves the `./media`/`./logs` bind mounts correctly — meaning the stack must be brought up from its root dir.
- **Network topology consolidation**: Replaced the mixed `sales_net` + `sales_internal` + `dlux_update_egress` layout with a clear 3+1 model: `frontend` (Caddy ingress only — a bridge, so the host reaches the app and Caddy can still reach Let's Encrypt/ACME for automatic HTTPS), `egress` (outbound internet: smtp-relay → Gmail, dlux-updater → PyPI, **celery** → rate scrapes cbl.gov.ly/eanlibya.com, composer-updater → Docker Hub), `internal` (no-internet inter-service: db, redis, web, celery, pgadmin, db-backup, Caddy backend), and an isolated `docker_proxy` (composer-updater ↔ docker-socket-proxy only). Same isolation guarantees as before (web still has no internet; celery keeps its egress only for the rate scrape), one obvious egress network instead of two.
- **Dev override + settings**: `compose.dev.yml` now mounts the live `pkg-django-lux/dlux` source into `smtp-relay` too (all app services now run the same editable dlux). `config/settings.py` splits the `dlux_settings` import onto its own line (with a `# noqa` note) so dlux_setup's literal-substring check sees it and stops re-appending its settings block.
- **Portfolio landing — real theme + language toggles**: The static `portfolio/index.html` previously *claimed* EN/AR + light/dark but had no controls (light/dark was OS-`prefers-color-scheme`-only and Arabic was a single hard-coded tagline). Added two header icon toggles: a **theme** switch (sun/moon) that sets an explicit `data-theme` on `<html>` — overriding the OS preference, persisted in `localStorage` (`sl_theme`), applied pre-paint via a tiny `<head>` script to avoid FOUC, and it still tracks live OS changes until the visitor picks one — and a **language** switch (`ع`/`EN`) that swaps the entire page via a `data-i18n`/`data-i18n-html` dictionary (50 keys × EN+AR), flipping `<html lang>`/`dir` for full RTL (Arabic system fonts + reset of Latin letter-spacing), persisted as `sl_lang`. Removed the standalone Arabic `.rtl` tagline (the lede now translates in place). Polish: `scroll-margin-top` for the sticky-header anchor offset, `:focus-visible` rings, theme-change color transitions, `prefers-reduced-motion` guard, toggles stay visible on mobile. Stays a host bind-mount (not baked into the image) by request.

## v0.1.3
- **Split edge into ERP subdomain + static public landing (per-hostname Caddy routing)**: The system is now served on **`erp.switchlibya.ly`** while the apex `switchlibya.ly` / `www.switchlibya.ly` serve a separate **static portfolio** — routed entirely at the Caddy edge (no Django, no dlux public-root feature). The `Caddyfile` moved the full ERP routing into an `(erp_app)` snippet imported by the `{$CADDY_ERP_ADDRESS}` site block, and added a second `{$CADDY_SITE_ADDRESS}` block that `file_server`s `./portfolio` (mounted read-only at `/srv/portfolio`), each hostname getting its own auto Let's Encrypt cert. **Breaking (prod .env):** the ERP host var changed from `NGINX_SERVER_NAME` → **`CADDY_ERP_ADDRESS`**; `CADDY_SITE_ADDRESS` now means the *portfolio* host(s). Defaults keep dev working (`compose.dev.yml` sets `CADDY_ERP_ADDRESS=:80` as the catch-all; the portfolio default `http://portfolio.localhost` never ACMEs and never collides). Updated `x-environment` defaults to `BASE_URL`/`ALLOWED_URLS=https://erp.switchlibya.ly` and `ALLOWED_HOSTS` to include `erp.switchlibya.ly` (apex/www never reach Django, so they're dropped from `ALLOWED_HOSTS`); `CSRF_COOKIE_DOMAIN`/`SESSION_COOKIE_DOMAIN` already track `BASE_HOSTNAME`, scoping ERP cookies to the subdomain. New `portfolio/index.html` is a self-contained, responsive, light/dark landing (smart-locks / access-control theme, EN + AR tagline, discreet "Staff Login → erp.switchlibya.ly"). `docker compose config` + `caddy validate` pass for dev, prod, and default configs. **Requires new DNS A records for `@`/apex and `www`** (only `erp` existed). Docs: `OPERATIONS.md` production-deploy section rewritten for the two-site model.
- **Catalog price fields stay consistent (form ↔ detail)**: The product detail view showed a **0.0 selling price** when only `cost_usd` + `markup_percent` were entered, because `price_usd` was left at 0 and derived only on the fly. `Product.save()` now **persists the derived USD selling price** (mirrors `effective_price_usd`) so detail views and every downstream read see a real number even on API/import/no-JS saves. A new `catalog/js/price_sync.js` (loaded on the Product/Service list pages via a reusable `ScopedListView.extra_scripts` hook, present when the create/edit modal opens) keeps the pricing fields in sync live as you type — edit **markup** → recomputes USD; edit **USD** → recomputes markup; edit **cost** → recomputes USD (keeping markup); the live USD→LYD rate (injected as `data-usd-rate` on the LYD widget from `finance.get_current_rate`) is shown as the LYD field's **placeholder**. Per the "keep LYD live" decision the manual LYD field is left blank unless you deliberately type into it (which then back-fills USD + markup and becomes a real fixed override) — so a blank field still sells at the live black-market rate. Fields are matched via `data-price-{cost,markup,usd,lyd}` hooks; event delegation + a `MutationObserver` handle the AJAX-loaded modal.
- **Detail view shows the live LYD selling price + translated choice labels**: dlux's generic detail builder (`_build_generic_detail_context`) only renders stored model fields (so the *derived* LYD price never appeared) and calls `get_<field>_display()` (which returns the **untranslated** English choice label). Fixed both without forking dlux: `Product`/`Service` expose a computed **"Selling Price (LYD)"** row via `get_modal_context()` (`extra_detail_fields`), rendered by a project override of `templates/dlux/helpers/dynamic_modal_detail.html`; and `Product.get_unit_display` / `Service.get_service_type_display` / `StockMovement.get_movement_type_display` are overridden to return the dlux-translated label (`unit_*`/`svctype_*`/`mtype_*` keys) so the Arabic detail view shows e.g. "علبة" instead of "Box".
- **Translated field help text (project-wide)**: `set_field_attrs` localizes field *labels* but never `help_text`, so form descriptions rendered in English under Arabic. New `common/forms.translate_help_text()` (mirrors `translate_choice_fields`) resolves `help_<model>_<field>` (then generic `help_<field>`) from DLUX_STRINGS and is called from every `ModelForm` (catalog/finance/sales). Added AR/EN `help_*` keys for `product.{sku,markup_percent,price_usd,price_lyd_override}`, `stockmovement.quantity`, and `exchangerate.rate`, and rewrote the Product pricing help text to match the new sync behavior (e.g. "Leave blank to sell at the live rate; enter a value to fix the LYD price").
- **Edge switched from nginx+certbot to Caddy (automatic HTTPS)**: Replaced the `nginx` service and the (never-functional) `certbot` renew-loop sidecar with a single `caddy` service (`Caddyfile`, `caddy:latest`). The old setup deadlocked on deploy — nginx refused to boot because its `ssl_certificate` files didn't exist yet, so port 80 never served the ACME challenge, so certbot could never issue the first cert (and `certbot renew` no-ops with zero certs; the overridden entrypoint also made a one-off `certonly` impossible). Caddy obtains **and renews** Let's Encrypt certs itself on boot with zero one-off commands, which fits the composer "just `./start.sh`" model exactly. Certs/ACME account persist in the new `caddy_data` volume (`caddy_config` for runtime state). The `Caddyfile` ports every rule from the retired `.nginx/default.conf.template` (moved to `.xpose/`): gzip/zstd `encode`, immutable 1-year `Cache-Control` on `/static/*`, `/media/*` with a `404` block on `/media/dlux_backups/*`, `/pgadmin4/*` proxy with `X-Script-Name`, the dlux **maintenance-flag** short-circuit (`file` matcher on `/opt/dlux-runtime/state/maintenance` → `error 503`) and the `handle_errors` maintenance page (`maintenance.html`, served `503` for 502/503/504, mirroring nginx `error_page … =503`), request-body cap, and the `/health` → `web:8000/health/` route. Automatic HTTP→HTTPS redirect is added by Caddy. Fixed a latent bug where port 443 published as `${NGINX_PORT:-443}` (any `NGINX_PORT` collapsed 80 and 443 to one port) — ports are now `${HTTP_PORT:-80}`/`${HTTPS_PORT:-443}`. `CADDY_SITE_ADDRESS` reuses `NGINX_SERVER_NAME`, `CADDY_EMAIL` reuses `DEFAULT_FROM_EMAIL`, `CADDY_MAX_SIZE` (default `10MB`) replaces `NGINX_MAX_SIZE`; healthcheck runs `caddy validate`. `compose.dev.yml` points the override at `caddy` (plain HTTP on host `90`, `CADDY_SITE_ADDRESS=:80`, no ACME). Updated `docs/OPERATIONS.md` with a domain + automatic-HTTPS deploy section. **No ACME email is set** — the first cut defaulted `CADDY_EMAIL` to `webmaster@localhost`, which Let's Encrypt rejects (`invalidContact — Domain name needs at least one dot`), wedging issuance; since a Caddyfile `email`/`tls` directive also can't take an empty value, the directive was dropped entirely. Caddy issues + auto-renews certs with no contact address; expiry notices are opt-in via a `{ email … }` global block in the `Caddyfile`.

## v0.1.2
- **Fix — sidebar highlighted "Invoices" on every sales page**: The invoice list was mounted at the app root `/sales/`, so dlux's prefix-based sidebar active check (`path.startswith(item_url)`) lit up Invoices on `/sales/dashboard/`, `/sales/customers/`, etc. Moved it to `/sales/invoices/` (name unchanged, all `{% url %}` refs auto-update) and redirected bare `/sales/` → dashboard. Now each sales page highlights only its own sidebar entry.
- **Cash-deposit batch combobox on payments**: The invoice payment form's "Cash Deposit" dropdown is now a `<datalist>` search-and-add combobox (same pattern as the customer field): type a batch reference to link an existing deposit or create one on the fly (`PaymentCreateView._sync_deposit`, find-or-create by `reference`, case-insensitive). A deposit that carries payments is a **batch** whose amount auto-sums its linked payments — `CashDeposit.recalc_amount()` runs from `Payment.save()/delete()` (reassigning a payment recomputes both the old and new batch), and `CashDepositForm` locks the amount field (display-only) while it has linked payments. The link stays fully optional (blank reference → no deposit). Deposits remain an optional cash-reconciliation tool for field/hand-over collection; everyday counter sales just record a Payment.
- **Fix — inline payments silently failed**: `PaymentForm` included the required `paid_at` field, but the invoice-detail quick-pay form only renders amount/method/deposit — so every UI payment failed validation ("paid_at is required") and redirected without saving (a `302`, no record). Removed `paid_at` from the form; it now falls back to its model default (`timezone.now`) on save. Earlier tests created `Payment` rows directly, bypassing the form, so they missed it — added `PaymentFormTests` covering the form path.
- **Translated permission labels in the group manager**: Added `perm_<codename>` AR/EN keys to `catalog`/`finance`/`sales` `translations.py` for every model permission (CRUD) plus the custom ones (`issue_invoice`, `cancel_invoice`, `view_sales_report`, `manage_exchangerate`, `confirm_cashdeposit`). dlux's `custom_permission_str` only localizes the permission *verb* (`can_view`→عرض) and leaves the model name in English, and the grouped-permission widget resolves each checkbox via `perm_<codename>` first — so without these keys the Arabic group manager showed e.g. "عرض Customer". Now it reads "عرض العملاء". English falls back to Django's default label unchanged.
- **Fix — `ProductTable` stock cell crash on Django 6.0**: `render_stock_qty` called `format_html('<span>—</span>')` with no placeholder args, which Django 6.0's stricter `format_html` rejects (`TypeError: args or kwargs must be provided`). Only non-stock-tracked products hit this branch, so it surfaced once demo data added such a product; now passes the dash as an arg. Added a `ProductTableRenderTests` regression test.
- **Demo Data Seeder**: New `sales/management/commands/seed_demo.py` (`python manage.py seed_demo`, `--reset` to rebuild) seeds a full test dataset — two `ExchangeRate`s (official + parallel), three `Category`s, six `Product`s (with barcodes, opening stock via `StockMovement`, a low-stock pair and an LYD-override "unrelated goods" item), five `Service`s, four `Customer`s, six `Invoice`s spanning every status (draft/issued/partial/paid/cancelled) with `Payment`s, and two `CashDeposit`s (pending + confirmed). Reference data is idempotent via `get_or_create`; invoices/deposits seed only when none exist (or after `--reset`). Issued invoices go through `issue_invoice()` so stock draws down and totals freeze realistically.
- **Form-Only Dynamic Modals**: Added project-scoped `scoped_modal_manager` / `scoped_modal_delete` routes (`config/urls.py`) that render dlux `DynamicModalManagerView` with `show_table=False`, so Add/Edit modals show only the form — no embedded records table. `common/views.ScopedListView` now emits `add_modal_url` + `modal_base_url` + `modal_delete_url`, and the new `common/static/common/js/scoped_crud.js` intercepts DluxTable `dlux:record:{view,edit,delete}` events (calling `preventDefault()` to bypass the dlux full-page fallback that pointed at non-existent `/{app}/{id}/edit/` routes) to open the form-only modal via `dlux:dynamic_modal:open` (or POST the delete). All modal `ModelForm`s set `refresh_parent = True` so the list reloads after a save.
- **Dashboard External Reference Rates (CBL + Black Market)**: `finance/services.py` scrapes two USD→LYD sources server-side (stdlib `urllib`; CSP forbids a browser cross-origin fetch): the **official** rate from `cbl.gov.ly` (`fetch_cbl_usd_rate`, cached `finance:cbl_official_usd_rate`) and the **black-market** rate + trend arrow from `eanlibya.com` (`fetch_ean_usd_rate`, cached `finance:ean_black_market_usd_rate`); both cached with **no expiry** — a value only ever changes when a later scrape succeeds, so a prolonged source outage never wipes the last-known rate. New `finance/tasks.refresh_market_rates` Celery task refreshes both (each independent) via `config/celery.py` `beat_schedule` (every 3h) plus a `worker_ready` warm-up on boot. Because `web` is network-isolated (`switch_pos_internal`, `internal: true`), the scraping runs in **celery** — which is now also attached to the egress bridge `switch_pos_net` — and the web tier reads the rates cache-only from Redis. The dashboard exchange-rate card shows the in-house **custom** rate beside the **official (CBL)** and **black-market (EAN)** rates, with the black-market trend (↑/↓), source dates, and the custom-vs-black-market gap.
- **Customer Search-and-Add Combobox**: The invoice header replaces the separate customer FK dropdown + name field with one `<datalist>`-backed text input (`InvoiceForm.customer` → `HiddenInput`). Typing/selecting matches an existing `Customer`, sets the hidden FK, and autofills phone/address; `_InvoiceEditorView._sync_customer()` binds or creates the `Customer` on save and backfills its blank phone/address, so every entered customer is saved and autofills on re-entry.
- **Filters via `advanced_filter_helper`**: `ScopedListView.get_filterset` now drives every list filter through dlux `advanced_filter_helper`, reading a per-`FilterSet` `advanced_config` (view-overridable via `filter_config`). Each config puts the keyword search + primary dropdown in the pill-shaped primary row and the rest inside the collapsible **"More filters"** advanced section (e.g. invoice date range, `is_active`, `unit`, `status`). The list templates render the filter with the **`{% crispy filter.form %}` tag** (not the `|crispy` filter, which silently ignores `form.helper` — the reason the advanced layout wasn't appearing). Added AR/EN strings `label_keyword`/`label_is_active`/`label_barcode`/`label_customer_name`, `search_placeholder`, `filter_advanced_search_action`, `filter_from`/`filter_to`, so every search box gets a translated placeholder and no filter renders as an unlabeled "unknown" field, with no dangling advanced toggle.
- **Translated dropdown options (`translate_choices`)**: New `common/forms.translate_choice_fields()` localizes every static-choice dropdown's *option* labels through dlux `translate_choices` (`choice_<value>` DLUX_STRINGS keys), applied to all list filters (in `get_filterset`) and to the modal `ModelForm`s (Product/Service/StockMovement/ExchangeRate/CashDeposit/Payment). Added the full `choice_*` AR/EN key set (status/method/movement/service-type/source/unit/kind/yes-no-any). This also fixed a pre-existing bug where django_filters `ChoiceField` dropdowns still showed `---------` as the first option: `set_first_choice` set the label on `field.choices`, but the widget renders from `widget.choices` — the helper now writes the translated, placeholder-preserving choices onto the widget, so filter dropdowns finally show their label (e.g. "Status"/"الحالة") as the first option.
- **Product Barcode Column**: Surfaced the existing `Product.barcode` field as a column in `ProductTable` (already searchable via `ProductFilter`).
- **Tests**: Added `finance.CblRateScraperTests` covering USD-row parsing (disambiguated from the Canadian/Australian dollar rows) and the network-failure fallback.

## v0.1.1
- **Tag-Driven Releases & GitHub Release Automation**: Replaced the branch-triggered `docker-publish.yml` (which pushed `debeski/sales:latest` on every `main` push but never created a GitHub Release) with the `app-composer`-style two-workflow setup. `release.yml` now fires on `v*` tags: it verifies `tag == VERSION`, runs `scripts/smoke-test.sh` against a freshly built image, pushes multi-arch (amd64+arm64) `debeski/sales:<ver>` + `:latest`, and publishes a **GitHub Release** from this `CHANGELOG.md` section via `softprops/action-gh-release`. A pushed git tag now becomes a real Release instead of only showing under Tags.
- **CI Gate**: `ci.yml` runs Django `check` + the test suite (`config.settings_dev_sqlite`) and a Docker build + runtime smoke test on every push/PR to `main`.
- **Version Bump**: `VERSION` → `0.1.1` (the release workflow fails the build if the pushed tag and `VERSION` disagree).

## v0.1.0
- **Modular App Architecture**: Scaffolded three DjangoLux apps via `dlux startapp --register` with a strict dependency layering `finance ← catalog ← sales`. Added a non-app `common` package (`common/views.py`, `templates/common/scoped_list.html`) holding the reusable `ScopedListView` so every list page wires `SingleTableMixin` + `FilterView` + `setup_filter_helper` + dynamic-modal "Add" in a few lines.
- **Finance — Global Exchange Rate**: `ExchangeRate` (append-only USD→LYD history; newest row is the live rate, cached under `finance:current_usd_lyd_rate`) with `manage_exchangerate` permission. Central `finance/services.py` (`get_current_rate`, `usd_to_lyd`, `lyd_to_usd`, `quantize_lyd`) is the single source of conversion math; ships a `DEFAULT_RATE` fallback surfaced as "no rate set".
- **Finance — Cash Deposits**: `CashDeposit` (ايداع نقدي) with `pending/confirmed/rejected` workflow, `confirm_cashdeposit` permission, and a `CashDepositReviewView` confirm/reject action.
- **Catalog — Hybrid Pricing**: `Product` priced in USD (`cost_usd` + `markup_percent` → `effective_price_usd`) auto-converted to LYD live, with an optional per-item `price_lyd_override` (the agreed hybrid model). `Service` offerings (installation/maintenance/warranty/delivery) priced in USD, LYD, or "per job". `Category` grouping. Auto-generated `sku` (`P00001`).
- **Catalog — Stock Ledger**: `StockMovement` (in/out/adjustment) is the authoritative inventory ledger; `Product.stock_qty` is excluded from the form and only changes through movements (applied atomically with `F()` on insert). Low-stock detection via `reorder_level`.
- **Sales — Invoices**: `Invoice` with auto number (`INV-000001`), `draft/issued/partial/paid/cancelled` lifecycle, and a **frozen** per-invoice `exchange_rate` plus per-line frozen `unit_price_lyd` so historical totals never shift when the rate moves. `InvoiceItem` supports product, service, and free-text custom lines with snapshot descriptions. `Customer` (optional; walk-ins allowed) and `Payment` (auto-maintains `amount_paid`/status, optional link to a `CashDeposit`).
- **Sales — Invoice Editor & Lifecycle**: Full-page multi-line editor (`InvoiceItemFormSet`) with a vanilla-JS add-row, live line/subtotal totals, and product/service price auto-fill from an embedded price map. `sales/services.py` `issue_invoice` (snapshots the rate record, draws down stock) and `cancel_invoice` (restores stock), both transactional. Detail page with issue/cancel/print/record-payment actions; standalone print-ready HTML invoice (`invoice_print.html`) branded from `APP_CONFIG`.
- **Sales — Dashboard**: Stat cards (sales today/month, outstanding balance, live rate), recent invoices, low-stock list, and pending-deposit alert. Intended as the system home page.
- **Roles**: `python manage.py seed_roles` creates a "Sales Staff" group (technicians/delivery reps) limited to selling, issuing invoices, taking payments, and recording cash deposits — no pricing, rate, or catalog access. Owner runs as a Django superuser.
- **Bilingual (AR/EN)**: `DLUX_STRINGS` label/model/app keys for all three apps so forms, filters and navigation render in Arabic or English via DjangoLux discovery.
- **Security & Audit**: Every domain model extends `ScopedModel` (audit trail, soft-delete, optional scope isolation) and every view is `LoginRequiredMixin` + `PermissionRequiredMixin` (`raise_exception=True`). Custom permissions: `manage_exchangerate`, `confirm_cashdeposit`, `issue_invoice`, `cancel_invoice`.
- **Sales Reporting + XLSX Export**: `sales/reports.py` (`build_sales_report`, `build_sales_report_xlsx`) aggregates issued/partial/paid invoices over a date window — total/collected/outstanding, by-status, daily, top products, top services, and an invoice list. `SalesReportView` (HTML, date-range filter) + `SalesReportExportView` (multi-sheet `.xlsx` via openpyxl, styled headers + money formats). Gated by a new `sales.view_sales_report` permission; the file-download route is marked `sidebar_exclude` so only the report *page* appears in navigation.
- **Negative-Stock Guard**: `issue_invoice` now aggregates demand per tracked product across lines and refuses to issue (transactional `ValidationError` naming each shortage) if any product would go negative — drafts and stock stay untouched on failure.
- **Dev tooling**: `config/settings_dev_sqlite.py` overlay for running migrations/checks locally without Postgres/Redis (dev only).
- **Launch**: Verified the DjangoLux Docker stack (`compose.yml` + `compose.dev.yml`) builds and starts via `./start.sh -d` (composer orchestrator, DEV mode, app on `http://localhost:90`).
- **CI/CD — tag-driven Docker Hub + GitHub Releases**: `.github/workflows/ci.yml` (push/PR to `main`: Django check + tests on `config.settings_dev_sqlite`, plus a Docker build + `scripts/smoke-test.sh` runtime gate) and `.github/workflows/release.yml` (on `v*` tag: verify `tag == VERSION`, smoke-test, multi-arch amd64+arm64 build & push `debeski/sales:<ver>` + `:latest`, then a GitHub Release from the matching `CHANGELOG.md` section via `softprops/action-gh-release`). `scripts/smoke-test.sh` boots the image and applies all migrations on in-image SQLite. Auth via `DOCKERHUB_USERNAME`/`DOCKERHUB_TOKEN` secrets; see `docs/RELEASING.md`. Patterned on the `app-composer` setup.
- **Full AR/EN Localization**: Promoted `common` to an installed (model-less) app hosting `common/translations.py` — shared `label_<col>` table-header keys (DjangoLux's django-tables2 patch localizes headers via these at render time), choice-label keys (`status_*`/`method_*`/`mtype_*`/`svctype_*`/`unit_*`/`source_*`/`kind_*`), and `ui_*`/`page_*`/`pr_*` chrome. Rewrote every hand-written template (invoice list/editor/detail/print, dashboard, report) to pull text from `DLUX_STRINGS`; `ScopedListView` now resolves page titles from `page_*_key`s; tables localize choice cells via a `common.i18n.t` helper and `Invoice.status_label`/`InvoiceItem.kind_label`/`Payment.method_label` properties; the printed invoice flips to `dir="rtl"` in Arabic. Previously only form/filter field labels were translated.
